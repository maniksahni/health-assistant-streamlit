services:
  - type: web
    name: health-assistant-streamlit
    env: python
    plan: free
    region: oregon
    buildCommand: |
      pip install --upgrade pip
      pip install -r requirements.txt
    startCommand: streamlit run app.py --server.port $PORT --server.address 0.0.0.0 --server.headless true
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: STREAMLIT_BROWSER_GATHERUSAGESTATS
        value: "false"
      # Uncomment and set these in the Render dashboard or keep here only if you use Blueprint deploys
      # - key: OPENAI_PROVIDER
      #   value: openrouter
      # - key: OPENROUTER_API_KEY
      #   sync: false
      # - key: OPENAI_API_KEY
      #   sync: false
      # - key: ADMIN_PASSWORD
      #   sync: false
      # - key: DATABASE_URL
      #   sync: false
